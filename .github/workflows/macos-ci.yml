name: macos-ci

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate package
        run: /bin/zsh macos/tests/test.sh
